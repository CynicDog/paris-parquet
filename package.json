{
  "name": "paris-parquet",
  "private": true,
  "type": "module",
  "description": "A single HTML file that reads a parquet file and shows you what's in it. src/ is the source; index.html is a generated build artifact.",
  "scripts": {
    "build": "node scripts/compose.mjs",
    "lint": "biome check src/ tools/ scripts/",
    "lint:fix": "biome check --write src/ tools/ scripts/",
    "test": "node tools/run-tests.mjs"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.13"
  }
}
